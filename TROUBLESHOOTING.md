# Common Issues

## Problem: "... .values(...).map is not a function ..."

### Solution: re-install the latest LTS version of Node.js from https://nodejs.org/en/download

## Problem: "'next' is not recognized as an internal or external command"

### Solution: first confirm you are in the right directory, if that does not resolve the issue then run npm install to fix dependencies
